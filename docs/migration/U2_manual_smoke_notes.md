# U2 – Manual Smoke Notes

- Environment: not run on device/emulator in this session; UI checks documented from code review.
- Expected during test run: when sections empty and log toggle off, TestExecutionScreen shows "In attesa dei risultati..." card; toggling raw log shows log pane.
- Expected at completion: toggle switches between log pane and sections; if sections empty, shows "Dettagli non disponibili..." message.
- Saved report action delegates to repository; no regressions observed in code review.
