package com.app.miklink.core.data.remote.mikrotik

/**
 * MikroTikClient - Domain-facing wrapper for the underlying API client
 *
 * Responsibilities:
 * - Provide methods like getNeighborsForInterface, getLinkStatus, runCableTest
 * - Map DTOs returned by the network layer to Domain models
 *
 * NOTE: this is a placeholder with no implementation in EPIC A
 */
interface MikroTikClient {
    // Example method signatures that will be adapted in future epics
    // suspend fun getNeighbors(interfaceName: String): List<...>
}
