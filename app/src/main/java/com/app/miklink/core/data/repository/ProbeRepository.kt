package com.app.miklink.core.data.repository

import com.app.miklink.core.domain.socket.SocketTemplate

/**
 * ProbeRepository - Domain-facing repository contract for probe configuration
 */
interface ProbeRepository {
    // suspend fun getProbes(): List<ProbeConfig>
    // suspend fun getProbeById(id: Long): ProbeConfig?
}
